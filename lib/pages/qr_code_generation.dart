import 'package:flutter/material.dart';

class QRCodeGenerationPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SafeArea(
        child: Container(
          child: Text("FaceRecognitionPage"),
        ),
      ),
    );
  }
}
